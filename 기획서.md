# 자봐 — 기획서

## 1. 개요

| 항목 | 내용 |
|---|---|
| 프로젝트명 | 자봐 |
| 한 줄 정의 | 이메일을 읽어 **오늘의 소식 / 업무 / 영수증 / 보안 / 프로모션 / 기타** 6개 카테고리로 자동 분류·태깅해주는 로컬 웹앱 |
| 형태 | 빌드 과정 없는 정적 프론트(HTML/CSS/바닐라 JS) + FastAPI 백엔드, 한 포트(`uvicorn`)로 동시 서빙 |
| 데이터 소스 | 데모 샘플 데이터(기본) 또는 사용자 본인의 Gmail 계정(OAuth 연동, 선택) |
| 실행 환경 | 로컬 단독 실행. 누구나 자신의 Google Cloud 자격증명으로 자신의 머신에서 구동하는 범용 앱 |

## 2. 배경 및 문제 정의

받은편지함에는 결제 영수증, 뉴스레터, 회의/업무 알림, 로그인·인증 보안 메일, 프로모션 메일이 뒤섞여 들어온다. 사용자는 이 중 **업무에 필요한 정보(오늘의 소식/업무/영수증)** 를 빠르게 확인하고 싶어 하며, 나머지(보안 알림, 프로모션, 분류 안 되는 기타 메일)는 참고용으로 작게만 보면 충분하다. 이를 자동 분류 + 우선순위가 반영된 대시보드로 해결한다.

## 3. 목표

1. 메일을 규칙 기반으로 1차 분류하고, 애매한 메일만 선택적으로 LLM 보조 분류를 사용한다.
2. **보안 관련 메일(인증번호, 로그인 알림, 비밀번호 재설정 등)의 본문은 어떤 경우에도 외부 LLM에 전달하지 않는다.** 이 안전장치는 기본 ON이며 분류 로직 단계에서 즉시 확정해 LLM 호출 경로 자체에 들어가지 않도록 보장한다.
3. 첫 화면에서 사용자가 가장 자주 보는 카테고리(오늘의 소식/업무/영수증)를 크게, 나머지는 작게 보여줘 정보 탐색 비용을 줄인다.
4. 분류 결과가 틀렸을 때 사용자가 직접 카테고리를 수동으로 정정할 수 있게 한다.
5. 데모 모드로 별도 설정 없이 즉시 동작을 확인할 수 있고, 필요 시 실제 Gmail과 연동해 라벨을 직접 생성/적용할 수 있다.

## 4. 비목표 (Out of Scope)

- 다중 사용자/로그인 시스템, 영속 DB (분류 결과·수동 정정은 인메모리로만 보관, 미니프로젝트 범위)
- 모바일 앱, 푸시 알림, 실시간 동기화
- 세션에 연결된 Gmail MCP 도구로 실제 받은편지함에 라벨을 적용하는 것 — Gmail 연동은 사용자가 자신의 자격증명으로 로컬에서 구동하는 독립 기능으로 분리
- 분류 규칙 자체의 카테고리 확장(예: "오늘의 소식"에 일반 공지까지 포함) — 현재는 라벨/화면 강조 방식만 바꾸고 분류 규칙은 그대로 유지

## 5. 카테고리 정의

| 화면 표시명 | 내부 id | 색상 | 분류 신호 예시 | 첫 화면 노출 |
|---|---|---|---|---|
| 오늘의 소식 | `newsletter` | `#059669` | 뉴스레터, 구독, digest, `List-Unsubscribe` 헤더 존재 | 메인(큰 섹션) |
| 업무 | `work` | `#7c3aed` | 회의, 미팅, pull request, calendar invite, github/jira/slack 발신자 | 메인(큰 섹션) |
| 영수증 | `payment` | `#2563eb` | 결제, 영수증, 청구서, 주문확인, invoice, receipt, stripe/paypal/toss 발신자 | 메인(큰 섹션) |
| 보안 | `security` | `#e11d48` | 인증번호, 인증코드, 로그인 알림, 비밀번호 재설정, OTP | 하단 요약 |
| 프로모션 | `promotion` | `#ea580c` | 할인, 세일, 쿠폰, sale, coupon, free shipping | 하단 요약 |
| 기타 | `other` | `#6b7280` | 위 규칙에 안 걸리는 나머지 (LLM 보조 분류 대상) | 하단 요약 |

- 내부 id(분류 규칙·Gmail 라벨명 등 시스템 식별자)와 화면 표시명(`label_ko`)을 분리해, 화면 문구가 바뀌어도 분류 로직·테스트·기존 Gmail 라벨(`AX/payment`, `AX/newsletter` 등)이 깨지지 않도록 설계했다.
- "오늘의 소식"은 기존 `newsletter`(뉴스레터/구독 메일) 카테고리를 화면에서 부르는 이름일 뿐, 분류 대상 범위는 넓히지 않았다.

## 6. 분류 로직

### 6.1 우선순위

```
보안(SECURITY) → 영수증(PAYMENT) → 업무(WORK) → 프로모션(PROMOTION) → 오늘의 소식(NEWSLETTER) → 기타(OTHER → LLM 보조, 설정 시)
```

### 6.2 동작 방식

1. **규칙 기반 1차 분류** — 제목/본문 스니펫의 키워드, 발신자 도메인 패턴, `List-Unsubscribe` 헤더 존재 여부를 점수화해 가장 높은 점수의 카테고리를 채택한다.
2. **보안 우선 확정** — 보안 키워드가 하나라도 매칭되면 다른 규칙을 평가하지 않고 즉시 `SECURITY`로 확정해 반환한다. 이 메일은 이후 어떤 LLM 호출 경로에도 들어가지 않는다(`needs_llm_fallback()`에서도 한 번 더 명시적으로 차단해 이중으로 보장).
3. **LLM 보조 분류(선택)** — 규칙에 전혀 매칭되지 않은(`OTHER`, `no_rule_matched`) 메일만, `USE_LLM_FALLBACK=true`이고 `ANTHROPIC_API_KEY`가 설정된 경우에 한해 Claude(`claude-haiku-4-5`)로 보조 분류한다. API 키가 없으면 항상 규칙 결과(`OTHER`)를 그대로 사용한다.
4. **신뢰도/근거 노출** — 모든 분류 결과는 `category`, `confidence`, `matched_rule`을 포함해 대시보드에 "근거: 키워드1, 키워드2 (95%)" 형태로 표시한다.
5. **수동 정정** — 사용자가 카드의 카테고리 드롭다운에서 직접 카테고리를 바꾸면 해당 이메일 id에 대해 `manual_override`로 고정되며, 이후 재분류 요청에서도 사용자가 지정한 카테고리가 우선한다.

## 7. 화면 설계

### 7.1 첫 화면(전체 탭, 기본값)

- **상단 바**: 앱 이름("자봐"), 데이터 소스 선택(데모/실제 Gmail), "Gmail에 라벨 적용" 체크박스, "분류 실행" 버튼.
- **요약 칩**: 전체 건수 + 카테고리별 건수 칩(카테고리 색상 적용).
- **카테고리 탭**: 전체/오늘의 소식/업무/영수증/보안/프로모션/기타 — 클릭 시 해당 카테고리만 모은 평면 리스트로 전환.
- **메인 3섹션(3열 카드, 좁은 화면에서 1열)**: 오늘의 소식 → 업무 → 영수증 순서로 각각 큰 카드 섹션을 그리고, 섹션 안에 해당 카테고리 이메일을 카드(제목/발신자/날짜/본문 일부/태그/근거/정정 드롭다운)로 나열.
- **하단 "기타 알림" 요약**: 보안/프로모션/기타 카테고리에 속한 이메일을 컴팩트한 한 줄(작은 태그+제목+발신자+정정 드롭마운)로만 표시해 메인 3개보다 시각적으로 작게 노출.

### 7.2 특정 카테고리 탭 선택 시

- 기존과 동일하게 해당 카테고리의 이메일만 모은 평면 카드 리스트를 보여준다(메인/기타 구분 없이 동일한 카드 컴포넌트 재사용).

### 7.3 공통 컴포넌트

- 이메일 카드: 제목, 발신자·날짜, 본문 스니펫(2줄 말줄임), 카테고리 색상 태그, 분류 근거(`matched_rule`+신뢰도 %), 카테고리 수동 정정 드롭다운.
- 에러 발생 시(예: Gmail 미설정 상태에서 "실제 Gmail" 선택) 상단에 에러 배너로 안내.

## 8. 시스템 아키텍처

```
backend/
  app/
    main.py          FastAPI 앱 + API 라우트 + 프론트엔드 정적 서빙 (StaticFiles)
    classifier.py    규칙 기반 분류 + Claude API 보조 분류
    gmail_client.py  Gmail API 연동 (메일 조회, 라벨 생성/적용)
    schemas.py       Pydantic 모델 (Category, CATEGORY_META, MAIN/MINOR_CATEGORY_ORDER, EmailMessage 등)
    sample_data.py   데모용 샘플 이메일 12건 (카테고리별 2건)
    config.py        환경 설정 (.env 기반 pydantic-settings)
  scripts/
    gmail_auth.py    최초 1회 Gmail OAuth 인증 스크립트
  tests/
    test_classifier.py   카테고리별 규칙 매칭 + 보안 메일 LLM 차단 테스트
frontend/
  index.html / styles.css / app.js   카테고리 필터, 메인/기타 섹션 구분, 통계, 수동 정정 대시보드
```

- 프론트엔드는 빌드 도구 없이 FastAPI의 `StaticFiles(html=True)` 마운트로 같은 포트에서 서빙된다.
- 메인/기타 카테고리 구분은 백엔드의 `MAIN_CATEGORY_ORDER`/`MINOR_CATEGORY_ORDER` 상수에서 계산한 `CategoryInfo.is_main` 플래그로 내려주고, 프론트는 이를 필터링만 한다(순서/구성 로직을 프론트와 백엔드에 중복 정의하지 않음).
- 분류 결과·수동 정정은 인메모리 `dict`로만 보관한다(미니프로젝트 범위상 DB 미사용, 서버 재시작 시 초기화).

## 9. API 명세

| Method | Path | 설명 |
|---|---|---|
| GET | `/api/health` | 헬스 체크 |
| GET | `/api/categories` | 카테고리 메타데이터(`id`, `label_ko`, `color`, `is_main`) — `MAIN_CATEGORY_ORDER + MINOR_CATEGORY_ORDER` 순서로 반환 |
| GET | `/api/emails?source=demo\|gmail&category=&limit=` | 분류된 메일 목록 조회 |
| POST | `/api/classify` | `{source, limit, apply_labels}` — 메일 조회 → 분류 실행. `source=gmail`이고 `apply_labels=true`일 때만 실제 Gmail 라벨 생성/적용 |
| POST | `/api/emails/{id}/override` | `{category}` — 사람이 카테고리를 수동 정정 (인메모리 캐시에 보관) |

## 10. Gmail 연동 (선택 기능)

1. Google Cloud Console에서 프로젝트 생성 + Gmail API 활성화 → OAuth 클라이언트(데스크톱 앱) JSON을 `backend/credentials.json`으로 저장.
2. `python scripts/gmail_auth.py` 최초 1회 실행 → 브라우저 동의 → `backend/token.json` 생성.
3. 대시보드 상단 소스를 "실제 Gmail"로 변경 후 "분류 실행".
4. "Gmail에 라벨 적용" 체크 시 `AX/{카테고리 id}`(예: `AX/payment`, `AX/newsletter`) 라벨이 실제로 생성·적용된다.
5. `credentials.json`, `token.json`은 `.gitignore`에 포함되어 커밋되지 않는다.

## 11. 보안/프라이버시 설계

- `SECURITY_LLM_BLOCK` 안전장치는 기본 `true`. 보안 카테고리로 확정된 메일은 분류 함수 내부에서 즉시 반환되어 LLM 호출 경로에 도달하지 않으며, `needs_llm_fallback()`에서도 한 번 더 카테고리를 검사해 이중으로 차단한다.
- `test_security_email_never_needs_llm_fallback` 테스트로 이 보장을 회귀 검증한다.
- `ANTHROPIC_API_KEY`가 없거나 `USE_LLM_FALLBACK=false`면 LLM 호출 자체가 발생하지 않고 항상 규칙 기반 결과만 사용한다.

## 12. 테스트 및 검증

- `backend/tests/test_classifier.py`: 카테고리별(결제/뉴스레터/프로모션/업무/보안) 규칙 매칭 테스트, `OTHER` 분류 시 `needs_llm_fallback=True` 테스트, 보안 메일은 절대 LLM 보조 분류 대상이 되지 않는다는 테스트 — 총 7개, 카테고리 표시 라벨이 바뀌어도 내부 `Category` enum을 기준으로 검증하므로 영향 없음.
- API 레벨 확인: `/api/categories` 응답에서 `label_ko`(영수증/오늘의 소식 등)와 `is_main` 플래그, 정렬 순서를 curl로 검증.
- 화면 레벨 확인: Playwright로 기본 화면(메인 3섹션 + 하단 요약), 카테고리 탭 클릭 시 평면 리스트 전환, 수동 정정 드롭다운 동작을 스크린샷으로 검증.

## 13. 실행 방법 (데모 모드)

```bash
cd backend
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
uvicorn app.main:app --reload
```

브라우저에서 `http://localhost:8000` 접속 → "분류 실행" 클릭 시 샘플 이메일 12건이 카테고리별로 태깅되어 표시된다. API 키나 Gmail 설정 없이 바로 동작한다.

## 14. 향후 고려 사항 (확정되지 않은 아이디어)

- 분류 규칙 추가/수정 UI (현재는 `backend/app/classifier.py` 직접 수정).
- 보안 LLM 차단 설정을 화면에서 직접 끄고 켤 수 있는 토글 (현재는 `.env`의 `SECURITY_LLM_BLOCK`으로만 제어).
- 분류 결과·수동 정정 영속화(DB 도입) — 현재는 미니프로젝트 범위상 인메모리로 한정.

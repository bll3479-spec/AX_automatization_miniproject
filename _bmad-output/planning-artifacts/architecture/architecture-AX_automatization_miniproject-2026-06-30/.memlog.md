---
scope: 자봐 PRD(F1-F4) 구현을 위한 아키텍처 스파인 — 분류 규칙 보강, 신규 카테고리, 진행률 UI, 일괄 적용 플로우
purpose: build-substrate + 비개발자용 요약 자료
altitude: feature
updated: 2026-06-30T08:05
---

- (decision) [ADOPTED] 파라다임 = 씬 FastAPI 라우트 + 무상태 규칙테이블 분류 파이프라인(classify_with_fallback) + 단일 in-memory override 딕셔너리(main.py:_overrides). 프론트엔드는 단일 전역 state 객체 + render()가 state로부터 전체 재렌더링하는 구조. 기존 코드에서 그대로 채택, 새로 발명하지 않음.
- (constraint) SECURITY 이중 가드(classifier.py:87-93 즉시 return, 127-135 needs_llm_fallback 재차단)는 어떤 리팩토링에도 분리되거나 약화되면 안 됨 — PRD NFR-1과 동일, 스파인에서도 AD로 고정.
- (constraint) _RULE_TABLE의 (Category, KEYWORDS, SENDERS) 튜플 순서가 동점 시 우선순위를 결정 — score 계산식(len(kw_hits)+len(sender_hits)*2, NEWSLETTER List-Unsubscribe +2)은 PRD NFR-2에 의해 변경 금지. 신규 카테고리는 테이블에 행을 추가하는 것으로만 확장.
- (decision) F3(FR-8) 배치 설계: 사용자가 '메일 목록은 1회만 가져오고 분류만 배치로 반복'을 선택. 이메일 fetch(소스 조회)와 classify(규칙+LLM 분류, 라벨적용)를 별도 호출로 분리 — fetch 1회 + classify-batch N회. 배치 호출은 이미 가져온 이메일 목록의 부분집합을 입력으로 받고, source를 재조회하지 않는다.
- (decision) F4(FR-10~12) 일괄 적용 설계: 사용자가 '기존 단건 override 엔드포인트 반복 호출 + 재분류는 마지막 1번만'을 선택. POST /api/emails/{id}/override는 시그니처 변경 없이 유지, 프론트엔드가 스테이징된 건수만큼 순차 호출 후 재분류는 F3의 classify-batch 경로를 재사용해 1회만 트리거 — 이때도 source를 재조회하지 않고 이미 메모리에 있는 raw 이메일 목록을 재사용한다(AD-1과 결합).
- (version) 스택은 기존 backend/requirements.txt에서 그대로 채택(신규 의존성 추가 없음): fastapi==0.115.0, uvicorn[standard]==0.30.6, pydantic==2.9.2, pydantic-settings==2.5.2, google-api-python-client==2.149.0, google-auth==2.35.0, anthropic==0.39.0, pytest==8.3.3, httpx==0.27.2. 프론트엔드는 빌드 없는 vanilla HTML/CSS/JS 유지.
- (event) Reconcile: PRD/project-context.md 대조 완료, 누락 없음 확인
- (event) lint_spine.py: low severity 1건('{id}'는 FastAPI 경로 파라미터 표기, false positive로 판정·수정 불필요)
- (decision) Reviewer Gate(버전검증/적대적 다이버전스) 적용: state.rawEmails 필드를 Consistency Conventions에 명시해 AD-3/AD-4의 'raw 목록 재사용' 요구가 구현 단계에서 갈리지 않도록 보강
- (event) spine finalized

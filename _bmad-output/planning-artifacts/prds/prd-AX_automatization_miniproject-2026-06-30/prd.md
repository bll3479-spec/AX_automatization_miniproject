---
title: '자봐 분류 규칙 보강 및 기능 확장 PRD'
status: final
created: '2026-06-30'
updated: '2026-06-30'
---

# PRD: 자봐 분류 규칙 보강 및 기능 확장

## Vision

자봐는 사용자가 매일 메일함을 직접 훑어보며 분류가 맞는지 재확인할 필요 없이, 대시보드의 분류 결과만 보고 바로 무엇을 처리할지 판단할 수 있는 신뢰도를 제공한다. 결제 영수증을 프로모션으로 놓치는 식의 오분류, 빈 진행 상황, 느린 수정 흐름은 모두 이 신뢰를 깨는 요인이다.

## Who This Serves

자봐를 자신의 Gmail 계정에 연동해 쓰는 1인 사용자(단일 운영자) — 별도 페르소나/유저저니 섹션 없이 진행한다.

## Features

### F1. 분류 규칙 정확도 보강

브리프 사례 1·2·3에서 확인된 실제 오분류를 `classifier.py`의 키워드/발신자 리스트 확장만으로 해결한다. 점수 계산 알고리즘 자체는 변경하지 않는다.

- **FR-1**: `PROMOTION_SENDERS` 리스트를 신설하고 최소 `"promo"` 패턴을 포함해 `_RULE_TABLE`의 PROMOTION 항목에 반영한다 (사례 1: `promos@wellness.iherb.com`).
- **FR-2**: `PAYMENT_KEYWORDS`에 정기결제/멤버십 계열 키워드(예: "정기결제", "멤버십")를 추가해 사례 3(쿠팡 와우 멤버십 영수증이 PROMOTION으로 역전되는 문제)을 완화한다.
- **FR-3**: `tests/test_classifier.py`에 사례 1·2·3 각각의 회귀 테스트를 추가한다. 사례 2는 코드 변경이 없는 "정상 동작" 케이스이므로, List-Unsubscribe 메커니즘이 의도대로 동작함을 고정하는 회귀 테스트로 포함한다.

> **알려진 한계 (모니터링 대상, 이번 범위 아님)**: `WORK_KEYWORDS`의 "마감"이 프로모션 카피("세일 마감임박")에도 흔히 등장해, PROMOTION 키워드가 1개뿐인 메일에서는 WORK로 잘못 분류될 위험이 남아있다(브리프 Known Limitations 참고). 이번 범위는 FR-1·FR-2의 키워드/발신자 보강으로 완화하되, 근본 해결(키워드 가중치 차등 등)은 NFR-2에 의해 이번 범위에서 제외된다.

### F2. 신규 카테고리 "보고서 및 간행물"

공공기관 발간물 메일(국회예산정책처, 서울시)이 OTHER로 빠지지 않도록 새 카테고리를 추가한다.

- **FR-4**: `Category` enum에 신규 항목 `report`(한글 라벨 "보고서 및 간행물")를 추가한다.
- **FR-5**: 신규 `*_SENDERS` 리스트를 만들고 최소 `"seoul.go.kr"`, `"nabo.go.kr"` 패턴을 포함해 `_RULE_TABLE`에 반영한다 (addendum.md 사례 4 증거 기준). `nanet.go.kr`(국회도서관)은 실증된 수신 메일이 없어 이번 범위에서 제외한다.
- **FR-6**: `CATEGORY_META`에 라벨/색상을 등록하고, 빈도가 낮은 카테고리이므로 `MINOR_CATEGORY_ORDER`에 배치한다.
- **FR-7**: `tests/test_classifier.py`에 사례 4 회귀 테스트를 추가한다. 발신자 매칭이 PROMOTION 키워드 충돌(예: `inews11@seoul.go.kr`의 "15% 할인" 메일)을 이기는지도 함께 검증한다.

### F3. 분류 진행률 UI

다건 분류 처리 중 사용자가 진행 상황을 알 수 있게 한다.

- **FR-8**: 프론트엔드가 다건 분류 처리 중 진행률(%)을 표시한다. 별도 서버 인프라(WebSocket/SSE) 없이, 프론트엔드가 `/api/classify` 호출을 일정 단위(batch)로 나눠 보내고 "처리된 건수/전체 건수"로 진행률을 계산하는 방식으로 구현한다 — 백엔드 `/api/classify`에 배치 단위 처리 지원 추가가 필요하다(`project-context.md`의 "빌드 도구 없음" 설계 전제와 정합).
- **FR-9**: 분류가 완료되면 "분류 완료" 표시를 노출한다(버튼 근처 또는 팝업).

### F4. 수동 정정 일괄 적용 플로우

여러 건의 카테고리 수동 정정을 모았다가 한 번에 반영해, 건마다 전체 재분류가 일어나지 않게 한다.

- **FR-10**: 프론트엔드는 override 변경 사항을 로컬에 스테이징하고, 변경 즉시 백엔드를 호출하거나 `runClassify()`를 재실행하지 않는다.
- **FR-11**: 사용자가 명시적으로 "적용" 액션을 실행했을 때만 스테이징된 변경 사항 전체를 백엔드에 반영하고, 단 한 번만 재분류를 실행한다.
- **FR-12**: 적용되지 않은 변경 사항이 있을 때 이를 시각적으로 표시한다(예: "적용" 버튼 활성화 또는 배지).

## Non-Functional / Cross-Cutting Constraints

- **NFR-1**: SECURITY 이중 가드(`classifier.py:87-93`, `133-134`)는 어떤 변경에도 약화되지 않는다.
- **NFR-2**: 분류 점수 계산 알고리즘(가중치, List-Unsubscribe 보너스)은 변경하지 않는다.
- **NFR-3**: 기존 `test_classifier.py` 7개 테스트는 회귀 없이 통과해야 한다.
- **NFR-4**: LLM 폴백 로직은 변경하지 않는다.

## Success Metrics

- 브리프 사례 1·2·3·4가 모두 올바른 카테고리로 분류된다.
- 기존 테스트 7개 + 신규 회귀 테스트(사례 1~4, 총 4건 이상) 전부 통과한다.
- 다건 분류 실행 중 진행률(%)이 표시되고, 완료 시 완료 표시가 노출된다.
- 여러 건의 수동 정정을 모아 한 번에 적용했을 때, 매 건마다 전체 재분류가 일어나지 않는다.

## Out of Scope (다음 라운드로 연기)

- PAYMENT → "결제 내역(영수증)" / "결제 수단 변경" 하위 카테고리 분리 — 데이터 모델·분류기·프론트엔드 3개 레이어 동시 변경 필요
- 사용자 지정 카테고리 추가 기능 — 영구 저장이 필요해 현재 앱의 "DB 없음" 설계 전제와 충돌
- SECURITY 이중 가드 변경, 점수 계산 방식 변경, LLM 폴백 로직 변경

## Open Questions

(없음 — 이전 초안의 [ASSUMPTION] 항목 3건 모두 합리적 기본값으로 확정해 FR-4·FR-5·FR-6·FR-8에 반영했다. 결정 근거는 `.memlog.md` 참고.)

- "국회도서관"(`nanet.go.kr`) 발신자 패턴은 향후 실제 메일 수신 시 추가 검토 대상으로 남는다 (FR-5 비포함, 의도적).

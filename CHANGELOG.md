# 변경 내역 (Changelog)

이 프로젝트의 주요 변경 사항을 버전별로 기록한다. 형식은
[Keep a Changelog](https://keepachangelog.com/ko/1.0.0/)를 따르고,
버전 번호는 [Semantic Versioning](https://semver.org/lang/ko/)을 따른다
(`MAJOR.MINOR.PATCH` — 기능 추가는 MINOR, 버그 수정/문서는 PATCH).

## [Unreleased]

(다음 배포 전까지의 변경 사항을 여기에 먼저 쌓는다.)

---

## [1.4.1] - 2026-06-29

### Fixed
- 대량 메일(전체 조회 등)에 Gmail 라벨을 적용할 때 버스트 레이트리밋으로
  2~3건이 간헐적으로 실패하던 문제 수정. 재시도 간격을 지수 백오프(0.5s →
  1s → 2s → 4s, 최대 5회)로 늘렸고, 모든 재시도 후에도 실패하는 메일이
  있어도 전체 요청을 실패시키지 않고 `label_apply_failures` 건수로 알려주도록
  변경(이미 적용된 나머지 라벨은 보존).

---

## [1.4.0] - 2026-06-29

### Changed
- 실제 Gmail 연동 시 조회 속도 개선. `gmail_client.fetch_recent_emails`가 메일
  메타데이터를 건당 1회씩 순차 호출(N+1)하던 것을 Gmail API batch 요청(최대
  100건씩 묶음)으로 변경.
- 라벨 적용(`apply_label` → `apply_labels_batch`)도 동일하게 메일마다 순차
  호출하던 것을 batch 요청으로 변경. 새로 생성된 라벨의 전파 지연으로 일부가
  실패하면 실패한 것만 모아 재시도(최대 3회)하도록 유지.

---

## [1.3.0] - 2026-06-29

### Added
- 카테고리별 페이지네이션 추가. 한 페이지당 20건씩 표시하고, 하단에
  "이전/페이지 번호/다음" 버튼으로 이동할 수 있다.
  - 홈 화면의 메인 3개 섹션(오늘의 소식/업무/영수증) 각각 독립적으로 페이징.
  - 하단 "기타 알림"도 보안/프로모션/기타 카테고리별로 나눠 각각 페이징.
  - 특정 카테고리 탭을 눌렀을 때 보이는 평면 리스트도 동일하게 페이징.
- 새로 분류를 실행하거나(소스/건수 변경, 수동 정정) 할 때마다 모든 페이지는
  1페이지로 초기화된다.

---

## [1.2.1] - 2026-06-29

### Fixed
- 조회 건수(20/50/100/500/전체) 드롭다운을 바꿔도 "분류 실행" 버튼을 눌러야만
  적용되던 문제 수정. 드롭다운 값을 바꾸면 즉시 재조회하도록 변경.

---

## [1.2.0] - 2026-06-29

### Added
- 이메일 조회 건수를 20건 고정에서 20/50/100/500/전체 중 선택 가능하도록 변경.
- `gmail_client.fetch_recent_emails`에 `nextPageToken` 기반 페이지네이션 추가
  (이전엔 Gmail API를 한 번만 호출해 20건 이상 요청해도 의미가 없었음).
- "전체" 선택 시 무한 호출을 막기 위한 안전 상한(2000건) 추가.

(커밋: `121a5f2`)

---

## [1.1.3] - 2026-06-29

### Fixed
- Gmail 라벨을 새로 생성한 직후 곧바로 메일에 적용하면 Gmail API 전파 지연으로
  `labelId not found (400)` 오류가 발생하는 문제 수정. 라벨 생성 후 조회
  가능해질 때까지 재확인하고, 라벨 적용 자체에도 짧은 재시도를 추가.
- 디버깅 내역을 `DEBUGGING_LOG.md`로 별도 기록 시작.

(커밋: `be3332c`, `ddc30a5`)

---

## [1.1.0] - 2026-06-29

### Changed
- 앱 이름을 "자봐"로 변경 (타이틀/헤더/브라우저 탭 포함).
- 홈 화면을 오늘의 소식(뉴스레터)/업무/영수증(결제) 3개 메인 카드 섹션 +
  보안/프로모션/기타 하단 요약 리스트 구조로 리디자인.
- 카테고리 표시 라벨 변경: 결제 → 영수증, 뉴스레터 → 오늘의 소식
  (분류 규칙·카테고리 id는 변경 없음, 하위 호환 유지).

### Docs
- 기획서, SRS(소프트웨어 요구사항 명세서) 문서 추가.
- 기획서에 보안 검토 결과(추후 보완 예정) 섹션 추가.

(커밋: `768a166`, `da68ac4`, `69df1d1`, `086875e`)

---

## [1.0.0] - 2026-06-28

### Added
- 이메일 자동 분류 앱 최초 구현.
- 카테고리 6종: 결제 / 뉴스레터 / 프로모션 / 업무 / 보안 / 기타.
- 규칙 기반 1차 분류 + 애매한 메일만 Claude API로 보조 분류(LLM 폴백).
- 보안 카테고리로 분류된 메일은 LLM 호출 경로에서 완전히 제외(하드 차단).
- Gmail OAuth 연동(`scripts/gmail_auth.py`) 및 실제 라벨 생성/적용 기능.
- 빌드 과정 없는 정적 프론트엔드 대시보드(FastAPI 동일 포트 서빙).
- 분류기 단위 테스트 7종 (`backend/tests/test_classifier.py`).

(커밋: `666f72a`)
